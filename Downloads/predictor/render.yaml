services:
  - type: web
    name: crown-picks
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT app_simple:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
