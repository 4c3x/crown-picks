import 'package:flutter/material.dart';

class ViewEstate extends StatelessWidget {
  const ViewEstate({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('View Real Estate Page Content Here',
        style: TextStyle(
          fontSize: 24,
        ),),)
    );
  }
}